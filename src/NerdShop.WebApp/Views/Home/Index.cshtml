@model HomeViewModel

<partial name="_Carousel" />

<h2>Os produtos preferidos </h2>

<div class="row">
    @foreach(var product in Model.FavoriteProducts)
    {
        <partial name="_ProductSummary" for="@product" />
    }
</div>